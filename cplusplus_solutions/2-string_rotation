#include <iostream>

bool rotate(std::string original, std::string candidate)
{
    return original.length == candidate.length && candidate.contains(original*2); //need to fix
}

int main()
{
    rotate("ABCD","BCDA"); 
}

